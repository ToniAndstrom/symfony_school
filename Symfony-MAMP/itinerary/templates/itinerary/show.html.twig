{% extends 'base.html.twig' %}

{% block title %}Itinerary{% endblock %}

{% block body %}
    <h1>Itinerary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ itinerary.id }}</td>
            </tr>
            <tr>
                <th>Activity</th>
                <td>{{ itinerary.activity }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ itinerary.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_itinerary_index') }}">back to list</a>

    <a href="{{ path('app_itinerary_edit', {'id': itinerary.id}) }}">edit</a>

    {{ include('itinerary/_delete_form.html.twig') }}
{% endblock %}
